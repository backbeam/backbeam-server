extends layout

block content
  .row
    .col-lg-12
      form(action='#{req.baseUrl}/configuration/reload', method='post')
        h1 Configuration
          small
            |  
            button.btn.btn-primary.btn-xs(type='submit') Reload configuration
      hr

      pre= JSON.stringify(req.core.config, null, 2)
